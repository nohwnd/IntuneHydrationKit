name: IntuneLinuxBaseline Parity Check

on:
  schedule:
    - cron: '30 13 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: intunelinuxbaseline-parity
  cancel-in-progress: false

jobs:
  compare:
    name: Compare bundled IntuneLinuxBaseline templates
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.2.2

      - name: Compare IntuneLinuxBaseline templates
        id: comparison
        shell: pwsh
        run: |
          $null = New-Item -Path ./build -ItemType Directory -Force
          ./scripts/Update-LinuxBaselineTemplates.ps1 2>&1 |
            Tee-Object -FilePath ./build/intunelinuxbaseline-update.log

          $templatePathSpec = @('Templates/Compliance', 'Templates/LinuxScripts')
          $null = New-Item -Path ./build/intunelinuxbaseline-diff.txt -ItemType File -Force
          git diff --name-status -- $templatePathSpec |
            Add-Content -Path ./build/intunelinuxbaseline-diff.txt -Encoding utf8
          $null = New-Item -Path ./build/intunelinuxbaseline-diff.patch -ItemType File -Force
          git diff -- $templatePathSpec |
            Add-Content -Path ./build/intunelinuxbaseline-diff.patch -Encoding utf8

          $diffSummary = Get-Content -Path ./build/intunelinuxbaseline-diff.txt -Raw
          $hasDifferences = -not [string]::IsNullOrWhiteSpace($diffSummary)
          "has_differences=$($hasDifferences.ToString().ToLowerInvariant())" >> $env:GITHUB_OUTPUT

      - name: Create or update GitHub issue
        if: steps.comparison.outputs.has_differences == 'true'
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SERVER_URL: ${{ github.server_url }}
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: |
          $title = 'Update bundled IntuneLinuxBaseline templates'
          $issueBodyPath = Join-Path -Path $env:RUNNER_TEMP -ChildPath 'intunelinuxbaseline-issue.md'
          $diffSummary = Get-Content -Path ./build/intunelinuxbaseline-diff.txt -Raw

          @"
          The weekly IntuneLinuxBaseline parity check found drift after syncing bundled templates from upstream.

          ``````diff
          $diffSummary
          ``````

          Workflow run: $env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/actions/runs/$env:GITHUB_RUN_ID

          Review the workflow artifact for the full patch, then update ``Templates/Compliance/`` and ``Templates/LinuxScripts/`` as needed.
          "@ | Set-Content -Path $issueBodyPath -Encoding utf8

          $existingIssue = gh issue list `
              --state open `
              --search "`"$title`" in:title" `
              --json number `
              --jq '.[0].number // ""'

          if ($existingIssue) {
              gh issue comment $existingIssue --body-file $issueBodyPath
              Write-Host "Updated existing issue #$existingIssue."
          } else {
              gh issue create --title $title --body-file $issueBodyPath
          }

      - name: Upload comparison artifacts
        if: always()
        uses: actions/upload-artifact@v4.6.2
        with:
          name: intunelinuxbaseline-comparison
          path: |
            build/intunelinuxbaseline-update.log
            build/intunelinuxbaseline-diff.txt
            build/intunelinuxbaseline-diff.patch
          if-no-files-found: warn

      - name: Fail on drift
        if: steps.comparison.outputs.has_differences == 'true'
        shell: pwsh
        run: |
          Write-Error 'Bundled IntuneLinuxBaseline templates drifted from upstream.'
